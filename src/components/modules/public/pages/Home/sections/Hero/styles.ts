import styled from '@emotion/styled';
import variables from 'theme/styles/vars';

export default styled.div`
    .layout-container {
        .headline {
            text-align: center;
            font-size: 3em;
            font-weight: 600;
        }
    }
`;