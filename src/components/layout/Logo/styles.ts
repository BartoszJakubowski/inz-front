import styled from '@emotion/styled';

export default styled.div`
    .logo {
        text-decoration: none;
        font-weight: 600;
        font-size: 2em;

        .soccer {
            color: white;
        }

        .score {
            color: orange;
        }
    }
`;